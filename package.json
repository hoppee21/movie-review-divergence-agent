{
  "name": "movie-review-divergence-agent",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 127.0.0.1",
    "typecheck": "npm --prefix frontend run typecheck",
    "build": "npm --prefix frontend run build"
  }
}
